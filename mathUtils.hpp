#include <cmath>
#include <random>
#include <cfloat>

using namespace std;

const double PI = 3.141592;

double func(double x, double y); /* function we are attempting to optimize (minimize) */
double randomDouble(double min, double max);